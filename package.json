
{
  "name": "retail_sales_management",
  "private": true,
  "workspaces": [
    "backend",
    "frontend_new"
  ],
  "scripts": {
    "install": "echo root install",
    "start": "npm run start-backend",
    "start-backend": "cd backend && node src/index.js",
    "install-backend": "cd backend && npm install",
    "install-frontend" : "cd frontend_new && npm install"
  }
}
